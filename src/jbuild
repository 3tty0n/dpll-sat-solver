(ocamllex (lexer))

(menhir
 ((modules (parser))
))

(library
 ((name dpll)
  (modules (:standard \ main))
  (libraries (oUnit))))

(executable
 ((name main)
  (public_name dpll-sat-solver)
  (modules Main)
  (libraries (dpll))))
